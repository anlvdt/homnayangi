# Dining flow override

The requested dining-service direction overrides the deck-first layout for the chooser and result. Retain Vietnamese typography, warm earth surroundings and the existing food data. A cream menu surface and leaf-green primary action establish the service sequence: method → interaction → dish → ordering or methods.

Use progressive disclosure for preferences and provider selection. Every result has one primary order CTA and a clearly named return to methods. Keep the original game interactions; present their outcome on a shared food-focused surface. Reduced motion, focus restoration, 44px controls and a scrollable result on short viewports are required. Delivery opens external search; never represent it as a confirmed order.
